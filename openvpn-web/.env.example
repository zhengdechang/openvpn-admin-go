# 前端环境变量

# 后端 API 地址（含协议）。前端 Node 服务通过 rewrites 将同源 /api 反代到此地址，
# 浏览器只走同源 /api，无需 CORS。
# 本地开发：http://localhost:8085  容器内：http://backend:8085
BACKEND_URL=http://localhost:8085

# 可选：仅当不使用 /api 同源代理、改为浏览器直连后端时才需要设置
# NEXT_PUBLIC_API_URL=http://localhost:8085
